node_modules/
dist/
*.log
.env
.env.*
# transient discovery / runtime state
.fleet/
*.sqlite
*.sqlite-*
data/*.json
data/*.txt
!data/.gitkeep

# browser-search stack (cloned by scripts/setup-browser-search.mjs)
.browser-search/

# internal working notes and private prompts (not for publication)
.prompts/
SOLUTIONS.md
# credentials / caches
auth.json
.pi/agent/auth.json
coverage/
.DS_Store
# large saved-HTML imports (keep out of git)
*.censys.html

# scratch/temp files
_*

# legacy reference scripts (kept locally, not published)
legacy/
